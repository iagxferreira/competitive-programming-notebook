#include <bits/stdc++.h>
using namespace std;

long long arraySum(const vector<int>& v) {
    // TODO: solve
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}
