#include <bits/stdc++.h>
using namespace std;

vector<int> reverseArray(vector<int> a) {
    // TODO: solve
    return a;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}
